document.addEventListener("DOMContentLoaded", () => {
    const passwordInputs = document.querySelectorAll("input[type='password']");

    /* This will later add password-toggle and CapsLock notifyer */

});